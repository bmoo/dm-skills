# Instance — pass · every PC covered, nothing to rule on

## The roster

| PC | Flagged ability |
|---|---|
| Vex | Sentinel reach |
| Bram | Grapple |
| Sera | Counterspell |
| Nyla | Channel Divinity |

## The pre-pass output

- **Uncovered:** `[]`
- **Beat share:** `{Vex: 1, Bram: 1, Sera: 1, Nyla: 1}`

## What the page stages

Four annotated beats across the night — the gate fight (`Spotlight: aimed at Vex`),
the cistern climb (`Spotlight (scene): Bram — exploration`), the harbormaster's
parley (`Spotlight (scene): Sera — social`), and the rite at the flooded shrine
(`Spotlight (scene): Nyla — exploration`).

*(The floor of the pass class: the uncovered set is empty, so
`spotlight-coverage` has nothing to rule on and the row holds without any
judgement of defensibility at all.)*
