# Instance — fail · reachable vehicle, but uninterpretable (needs unseen content)

## The clue (in the deepened node — the mosaic hall)

> **The glyph floor.** Anyone who studies the mosaic (an Investigation check) sees the
> tiles spell a message in a glyph-cant — but the message only resolves once you hold
> the **Verdant Choir's cant-key**, the decoding table that pairs each glyph to a
> letter. Read with the key, the tiles name the hidden vault.

## The node context

- **Vehicle:** **present and reachable** — an Investigation check on the mosaic surfaces
  the tiles. Prong 1 (vehicle) holds. This instance isolates the *interpretability*
  prong.
- **What the players already know when they'd find it:** they have **not** visited the
  grove node where the **cant-key** is found, and have never seen the Choir's cipher.
  No glyph on this floor maps to anything they know. At the moment of finding, the
  message is unreadable noise — it requires unseen content to mean anything.

*(No party roster is relevant — `clue-interpretability` does not read it.)*
