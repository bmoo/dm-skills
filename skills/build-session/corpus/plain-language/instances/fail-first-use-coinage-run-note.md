# Instance — fail · coinage first-used in run-time text, never defined

## The line (run-time text — a keyed-area run note)

> **In the flooded nave:** when a light is struck, **the Forgotten** stir in the water
> and rise within 1 round.

## What the page defines nearby

- **"The Forgotten" appears only here.** No Key Plot Point, no clue payload, no earlier
  passage on the page names or defines them — the term is first-used **in run-time text
  itself** with no prior definition. A stranger DM reading the run note cannot tell who
  or what stirs: undead? townsfolk? a faction? There is no stat-block reference either.
  The line rests on an undefined coinage introduced where it is used — the
  `plain-language` defect. *(Contrast the pass case, where "the candle-ledger" was
  defined on the page before the run note used it.)*
