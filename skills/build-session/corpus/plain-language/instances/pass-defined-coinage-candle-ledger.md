# Instance — pass · coined term defined earlier on the page

## The line (run-time text — a keyed-area run note)

> **In the counting-room:** if the party opens the **candle-ledger**, they read the
> next three drop sites and the name of the fence who receives them.

## What the page defines nearby

- The page's **clue slate**, earlier, defined the term: *"the **candle-ledger** — the
  smugglers' shipment book, kept by candlelight so it can be burned fast."* The coinage
  is introduced **before** it is used in this run note, so a stranger DM hits the run
  note already knowing what the candle-ledger is. A coined term is legal exactly when
  the page defines it before or where it's used.
