# Instance — edge · coinage lives in a design aside, not run-time text

## The line (the passage in question)

> *(a designer's note in the page's Key Plot Points section, set off in italics)*
>
> *Design intent: this whole node is about "thread the tide" — the party should feel the
> harbor closing around them before they see a single enemy.*

## What the page defines nearby

- **"Thread the tide" is an undefined metaphor** — if it appeared in a spotlight line or
  a contingency the DM executes mid-session, it would break `plain-language`. But here
  it sits in a **design-intent aside**, framing prose *about* the node for the prepping
  DM, not a line anyone runs at the table. `plain-language` is scoped to **run-time text
  only** — text the DM executes mid-session (encounter blocks, spotlight lines, run
  notes, contingencies). The doubt `plain-language`'s scope prong must settle: is this
  line run-time text, or design discussion where aphoristic compression is explicitly
  allowed? Deciding *which* text is run-time is itself part of `plain-language`'s
  judgement.
