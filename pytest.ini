[pytest]
# The mechanical checker lives at lib/mechanical-checker and materialises into
# skills by symlink (scripts/mechanical_checker). Collect its tests once, from
# lib/ -- recursing into the symlink would import every module twice.
norecursedirs = mechanical_checker
